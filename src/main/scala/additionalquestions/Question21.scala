package additionalquestions

object Question21 extends App {

    println("The reflection API allows an executing Scala program to examine or “introspect”" +
        "upon itself. Write a method which uses reflection to find out (and output) which" +
        "methods are defined within a class. You should include the formal parameter and" +
        "return types in your output together with any checked exceptions the methods may throw.")

    println("Reflection was coursework one so unlikely to be on the exam.")
}
