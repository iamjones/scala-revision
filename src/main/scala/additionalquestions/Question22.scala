package additionalquestions

object Question22 extends App {

    println("Which GOF patterns are classified as “structural”? Name them.")

    println("Composite, Facade, Proxy, Adapter, Decorator")

    println("What problem does the MVC pattern solve and how?" +
        "Describe what each of the Model, View, Controller components represent." +
        "Name an advantage that stems from using MVC pattern.")

    println("Im going explain it in terms of a web site." +
        "The View would be what is rendered to the user, so the HTML." +
        "The Controller should contain code that is responsible for getting data and" +
        "passing it to the view so it can be displayed to the user." +
        "The Model should contain the data that is needed by the application.")
}
